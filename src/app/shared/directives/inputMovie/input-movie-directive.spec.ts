import { InputMovieDirective } from './input-movie-directive';

describe('InputMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMovieDirective();
    expect(directive).toBeTruthy();
  });
});
